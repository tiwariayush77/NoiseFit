import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from '@/components/ui/card';

export default function GoalSelectionPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Set Your Goals</CardTitle>
        <CardDescription>Coming Soon</CardDescription>
      </CardHeader>
      <CardContent>
        <p>This is where users will set their fitness goals.</p>
      </CardContent>
    </Card>
  );
}
